name: Devops homework CI

on:
  push:
    branches: ["master"]
  pull_request:
    branches: ["master"]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Use Node.js 16.x
      uses: actions/setup-node@v3
      with:
        node-version: 16.x
        cache: 'npm'
    - name: Install dependencies
      run: npm ci
    - name: Lint
      run: npm run lint
    - name: Generate_tag
      run: echo "tag=$(date +%s)" >> $GITHUB_OUTPUT
    - name: Build the Docker image
      run: docker build . --file Dockerfile -t ${{ secrets.DOCKER_USERNAME }}/my-image:${{ steps.Generate_tag.outputs.tag }}
    - name: Login into docker
      run: docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }}
    - name: Push image
      run: docker push ${{ secrets.DOCKER_USERNAME }}/my-image:${{ steps.Generate_tag.outputs.tag }}
